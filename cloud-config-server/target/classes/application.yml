server:
  port: 8888
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/meharshitjain/microservices-using-springboot
          clone-on-start: true
